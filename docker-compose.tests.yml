version: '3.8'

services:
  test_app:
    build:
      context: .
      dockerfile: Dockerfile
    command: "pytest -vv"
    networks:
      - test_network


networks:
    test_network:
      external:
        name: web_network
